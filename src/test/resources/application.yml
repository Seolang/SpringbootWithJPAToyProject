# test 실행시 이 파일의 설정을 우선적으로 적용함
# 스프링 부트에서는 별도의 설정이 없으면 자동적으로 memory모드로 실행함!
spring:
#  datasource:
#    url: jdbc:h2:mem:test # Hibernate를 메모리에서 db를 띄우기 위한 설정
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver

#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    properties:
#      hibernate:
#        System out으로 출력됨
#        show_sql: true
#        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace